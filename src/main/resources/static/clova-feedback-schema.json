{
  "type": "object",
  "properties": {
    "summary": {
      "type": "string",
      "description": "전체 글에 대한 한 줄 요약"
    },
    "strength": {
      "type": "string",
      "description": "글의 장점 1가지"
    },
    "improvementPoints": {
      "type": "array",
      "description": "개선할 점 2가지를 담은 배열",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "string",
        "description": "개선할 점"
      }
    },
    "grade": {
      "type": "string",
      "enum": ["A", "B", "C", "D", "F"],
      "description": "글의 완성도 등급 (A: 최우수, F: 미흡)"
    }
  },
  "required": [
    "summary",
    "strength",
    "improvementPoints",
    "grade"
  ]
}